services:
  - type: web
    name: ledgerops-backend
    env: docker
    rootDir: backend
    plan: starter
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: ledgerops-postgres
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: ledgerops-redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: ML_SERVICE_URL
        value: https://ledgerops-ml.onrender.com
  - type: web
    name: ledgerops-ml
    env: docker
    rootDir: ml-service
    plan: starter
    healthCheckPath: /health
  - type: redis
    name: ledgerops-redis
    plan: starter
databases:
  - name: ledgerops-postgres
    plan: starter
